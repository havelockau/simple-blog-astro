---
import Layout from '../layouts/Page.astro'
import Container from '../components/Container.astro';
import BlogPostsList from '../components/BlogPostsList.astro';

const posts = (await Astro.glob('./blog/*.{md,mdx}')).sort(
	(a, b) => new Date(b.frontmatter.publishedAt).valueOf() - new Date(a.frontmatter.publishedAt).valueOf()
);

const seo = {
  title: 'HAVELOCK',
  description: 'A Perth, WA based international security and cybersecurity blog',
}
---

<style>
  .home-hero-text__container {
    padding: 5vh 0;
  }
  h1 {
    text-align: center;
  }
  h4 {
    font-size: 1em;
    text-align: center;
  }
</style>

<Layout seo={seo}>
  <Container>
    <div class="home-hero-text__container">
      <h1 class="fade-in">HAVELOCK</h1>
	    <h4 class="fade-in">Cybersecurity blog</h4>
    </div>
    <BlogPostsList posts={posts}/>
  </Container>
</Layout>

